version: '3.8'

services:
  web:
    build: .
    ports:
      - "5001:5001"
    env_file:
      - .env
    volumes:
      - database_data:/app/data
      - uploads_data:/app/public/pics
    restart: unless-stopped

volumes:
  database_data:
  uploads_data:

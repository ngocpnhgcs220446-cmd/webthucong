# WARNING: Always generate new passwords and secrets for production!
# DO NOT commit the real .env file into version control or share it.
DATABASE_URL="file:./dev.db"
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-password
JWT_SECRET=change-this-long-random-secret
PORT=5001
NODE_ENV=production

SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
ADMIN_NOTIFY_EMAIL=

CLOUDINARY_URL=
